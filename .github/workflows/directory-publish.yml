name: 'Publish to the Vaadin Directory'

env:
  ADDON: nps  
  VERSION: ${{ github.ref_name }}
  ADDON_ZIP: ${{ADDON}}-${{VERSION}}.zip

on:
  release:
    types: [published]
  
jobs:
  validation-build:
    uses: samie/nps/.github/workflows/validation-build.yml@v24
    
  publish:
    needs: [validation-build]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Set Version
      id: set-version
      run: ( cd addon && mvn versions:set -DnewVersion=${{VERSION}} )
    - name: Build with Maven
      run: ( cd addon && mvn -B package -Pdirectory --file pom.xml )
    - name: Upload to Vaadin Directory
      id: upload
      uses: wei/curl@v1
      continue-on-error: false  
      with:
        args: -X POST "https://vaadin.com/vaadincom/directory-service/upload/${{env.ADDON}}" -F "authKey=${{ secrets.DIRECTORY_AUTH_KEY }}" -F "publish=true" -F "releaseNotes=test" -H "accept:\ */*" -H "Content-Type:\ multipart/form-data" -F "file=@addon/target/${{env.ADDON_ZIP}}.zip;type=application/zip"
    - name: Report upload failure
      if: ${{ job.status == 'failure' }}
      run: echo '${{ toJSON(job) }}' ;  exit 1
